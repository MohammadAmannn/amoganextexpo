The complete contents of the user-provided `amogads-main(1).zip` are preserved under `apps/web/`. No source file from that archive was intentionally omitted. Root-level monorepo files are new orchestration/configuration.

Run `pnpm verify:preserved` to verify all 1,408 original files still match the uploaded source snapshot. The SHA-256 manifest is `PRESERVED_SOURCE_SHA256.txt`.
